<section id="menu">
		<ul>
                <li><a href="{{ MainROOT }}">Main Page</a>
            {{ FOREACH Floors.values.sort('url') }}
                 <li>
                     <a href="{{ url }}" name="{{ description }}">{{ name }}</a>
                 </li>
            {{ END }}
		</ul>
	</section>
